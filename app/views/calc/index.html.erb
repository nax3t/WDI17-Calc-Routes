<h1>Calculator Routes</h1>

<p>Type your equation into the url bar like so: localhost:3000/add/3/5 will return 8</p>
<!-- if an instance variable named @result exists (is true)... -->
<% if @result %>
	<!-- ...then display the contents of @result -->
	<h2>Result: <%= @result %></h2>
<% end %>